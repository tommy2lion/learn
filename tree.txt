test/
├── .gitignore
├── tree.txt                          # this file — project structure overview
│
├── webpage/                          # ① Web version (HTML / CSS / JS)
│   └── index.html                    #   Calc 24 game running in the browser
│
└── withC/                            # C implementations
    ├── Makefile                      #   builds all three targets
    │
    ├── terminal/                     # ② Character-mode version
    │   └── calc24.c                  #   ASCII cards, stdin/stdout, single file
    │
    ├── gui/                          # ③ Monolithic graphical version (raylib)
    │   └── calc24_gui.c              #   single-file GUI, kept as reference
    │
    ├── modular/                      # ④ Refactored modular version (raylib)
    │   ├── main.c                    #   entry point & main loop (~55 lines)
    │   ├── solver.h / solver.c       #   brute-force 24 solver  (no raylib)
    │   ├── parser.h / parser.c       #   expression evaluator   (no raylib)
    │   ├── game.h   / game.c         #   rules & state machine  (no raylib)
    │   ├── card.h   / card.c         #   card label + draw
    │   ├── ui.h     / ui.c           #   reusable widgets (Button, TextBox)
    │   ├── screen_game.h / .c        #   main play screen layout & events
    │   └── screen_custom.h / .c      #   custom-number modal screen
    │
    └── design/                       # Architecture documentation
        └── architecture.md           #   modularisation design & patterns
